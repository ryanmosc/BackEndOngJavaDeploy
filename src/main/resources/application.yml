spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/BackEndVoluntarios
    username: postgres
    password: q1w2e3
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

app:
  upload:
    dir: ${user.dir}/src/main/resources/static/uploads  # Pasta para imagens (crie manualmente)
